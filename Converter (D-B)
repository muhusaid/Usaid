#include <stdio.h>
#include <conio.h>

void main()
{
	int x,y,z;
	
	printf("Insert a Decimal No ");
	scanf("%d", &x);
	
	do
	{
		y=x/2;
		z=x%2;
		x=y;
		printf("%d\t",z);
	}
	while(x!=0);
	printf("loop End");
}
